import {int} from "../01/extra/numbers.js";
import {a,b,c,d,e,f,g,h,i,j,k} from "../01/extra/multiplier.js";

console.log(int);


const result = `
    ${a} * ${int} = ${(a * int)}
    ${b} * ${int} = ${(b * int)}
    ${c} * ${int} = ${(c * int)}
    ${d} * ${int} = ${(d * int)}
    ${e} * ${int} = ${(e * int)}
    ${f} * ${int} = ${(f * int)}
    ${g} * ${int} = ${(g * int)}
    ${h} * ${int} = ${(h * int)}
    ${i} * ${int} = ${(i * int)}
    ${j} * ${int} = ${(j * int)}
    ${k} * ${int} = ${(k * int)}
`;

console.log(result);